doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport', content='width=device-width, initial-scale=1')
  title Coming Soon | WeCode Pixel
  meta(name='description', content='WeCode Pixel is an emerging startup with group of well experienced IT People based out from Banglaore, INDIA.')
  meta(name='keywords', content='startup template, web development, designing, maintainance, SEO, Hosting consulting, wordpress')
  meta(name='author' content='wecode')
  link(href='' rel='shortcut icon')
  link(type='text/css' href='/stylesheets/normalize.css' rel='stylesheet')
  link(type='text/css' href='http://fonts.googleapis.com/css?family=Raleway:200,400,800' rel='stylesheet')
  link(href='https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap' rel='stylesheet')
  //if IE
    script(src='http://html5shiv.googlecode.com/svn/trunk/html5.js')
  link(type='text/css' href='/stylesheets/styles.css' rel='stylesheet')
#large-header.large-header
  canvas#demo-canvas
#Content.wrapper
.top-wrapper
  //- .navbar-header
  //-   a.navbar-brand(href='#')
  //-     | WeCode
  #header
    .wrapper
      h1
        a.logo(href='#') WeCode Pixel
      #countdownstyled.countdown.styled
  .bottom-wrapper.wrapper
    h2.intro
      | We have been working on something Amazing. 
      br
      | We will be back soon.
    #subscribe
      form(method='POST' action='')
        p.subscribe-input
          input(name='Email' type='text' size='20' value='' placeholder='Subscribe your e-mail Id')
          input(name='Submit' type='submit' value='Submit')
      #socialIcons
        ul
          li
            a.twitterIcon(href='#' title='Twitter')
          li
            a.facebookIcon(href='#' title='facebook')
          li
            a.linkedInIcon(href='#' title='linkedIn')
          li
            a.pintrestIcon(href='#' title='Pintrest')
script.
  // Set the date we're counting down to
  var countDownDate = new Date("Dec 31, 2019 23:59:59").getTime();
  // Update the count down every 1 second
  var x = setInterval(function() {
  // Get today's date and time
  var now = new Date().getTime();
  // Find the distance between now and the count down date
  var distance = countDownDate - now;
  // Time calculations for days, hours, minutes and seconds
  var days = Math.floor(distance / (1000 * 60 * 60 * 24));
  var hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
  var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
  var seconds = Math.floor((distance % (1000 * 60)) / 1000);
  // Output the result in an element with id="demo"
  document.getElementById("countdownstyled").innerHTML = ("<div>" + days + " <span>DAYS</span></div><div>" + hours + " <span>HOURS</span></div><div>" + minutes + " <span>MINUTES</span></div><div>" + seconds + " <span>SECONDS</span></div>");

  //- document.getElementById("countdownstyled").innerHTML =  days + "d " + hours + "h " + minutes + "m " + seconds + "s ";
  // If the count down is over, write some text
  if (distance < 0) {
  clearInterval(x);
  document.getElementById("countdownstyled").innerHTML = "EXPIRED";
  }
  }, 1000);

script(src='/javascripts/TweenLite.min.js')
script(src='/javascripts/EasePack.min.js')
script(src='/javascripts/rAF.js')
script(src='/javascripts/demo-1.js')
// Scripts
script(src='/javascripts/jquery-1.9.1.min.js' type='text/javascript')
//- script(type='text/javascript' src='/javascripts/Backstretch.html')
script(src='/javascripts/jquery.countdown.js' type='text/javascript')
script(src='/javascripts/global.js' type='text/javascript')